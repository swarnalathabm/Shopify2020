<p id="notice"><%= notice %></p>

<h1>Product Tags</h1>

<table border=1>
  <thead>
    <tr>
      
      <th>ProductCategory</th>
      <th>ProductGroup</th>
      <th>ProductCode</th>
      <th>ProductDescription</th>
      <th>ProductTag</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody >
    <% @product_tags.each do |product_tag| %>
      <tr>
        
        <td><%= product_tag.pcategory %></td>
        <td><%= product_tag.pgroup %></td>
        <td><%= product_tag.pcode %></td>
        <td><%= product_tag.pdescription %></td>
        <td><%= product_tag.ptag %></td>
        <!--<td><%= link_to 'Show', product_tag %></td> -->
       <!-- <td><%= link_to 'Edit', edit_product_tag_path(product_tag) %></td>-->
        <!--<td><%= link_to 'Destroy', product_tag, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product Tag', new_product_tag_path %>
